// The name of the function must match the name of the file!

// Every ES6(Arrow Function) React function must start with const
// Next we name the function
// Then we create a function ---  () => {}
const Card = () => {
    return (
        <div>
            <h1>Card is working!!!</h1>
        </div>
    )
}

// if it is the only function in the file we use export default
export default Card;